-- ============================================================
-- ИСТОРИЧЕСКАЯ ЗАПИСЬ, содержимое неактуально.
--
-- Этот файл когда-то добавлял 225 вопросов и чинил перекос позиции
-- правильного ответа (было 33/60/7/0 по индексам 0-3 — угадывалось
-- без знания ответа). Результат этой работы полностью вошёл в
-- supabase/questions/bank.sql, который переиздаёт финальное
-- состояние (после ещё одной переборки в 010) для ВСЕХ вопросов
-- одним файлом. Данные отсюда убраны, чтобы не было трёх разных
-- мест, с которыми надо сверяться на дубли при добавлении новых
-- вопросов — сверяться нужно с bank.sql (или напрямую с БД).
--
-- git log покажет исходное содержимое, если понадобится история.
-- ============================================================
