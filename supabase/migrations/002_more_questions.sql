-- ============================================================
-- Уникальность текста вопроса — нужна, чтобы дальнейшие insert'ы
-- вопросов (см. supabase/questions/bank.sql) были идемпотентны
-- через on conflict (question) do nothing.
--
-- Сами вопросы, которые исходно добавлялись здесь, теперь живут
-- в supabase/questions/bank.sql — единственном актуальном срезе
-- базы вопросов (см. комментарий в начале того файла: раньше они
-- были раскиданы по 002/008/010, и это мешало сверяться на дубли).
-- ============================================================

alter table public.questions
  add constraint questions_question_unique unique (question);
